# Як зробити внесок у проект

Ми будемо раді прийняти ваші виправлення та внески у цей проект. Цей документ
містить:

- **[Перш ніж почати](#перш-ніж-почати):** Необхідні кроки перед тим, як стати
  учасником розробки Gemini CLI.
- **[Процес внесення змін у код](#процес-внесення-змін-у-код):** Як
  запропонувати зміни до коду Gemini CLI.
- **[Налаштування середовища розробки](#налаштування-середовища-розробки):** Як
  налаштувати робоче середовище.
- **[Процес внесення змін у документацію](#процес-внесення-змін-у-документацію):**
  Як допомогти з документацією.

Ми з нетерпінням чекаємо на ваші ідеї та код!

## Перш ніж почати

### Підпишіть нашу Угоду про надання прав на інтелектуальну власність (CLA)

Внески до цього проекту повинні супроводжуватися
[Угодою про надання прав (CLA)](https://cla.developers.google.com/about). Ви
(або ваш роботодавець) зберігаєте авторські права на ваш внесок; це просто дає
нам дозвіл використовувати та розповсюджувати ваші зміни як частину проекту.

Якщо ви або ваш роботодавець уже підписали Google CLA (навіть для іншого
проекту), вам, ймовірно, не потрібно робити це знову.

Відвідайте <https://cla.developers.google.com/>, щоб переглянути поточні угоди
або підписати нову.

### Ознайомтеся з нашими Правилами спільноти

Цей проект дотримується
[Правил спільноти Google Open Source](https://opensource.google/conduct/).

## Процес внесення змін у код

### Як почати

Процес внесення змін у код виглядає так:

1.  **Знайдіть проблему (issue)**, над якою хочете попрацювати. Якщо проблему
    позначено як "🔒Maintainers only", це означає, що вона зарезервована для
    розробників проекту. Ми не прийматимемо pull request для таких проблем.
2.  **Зробіть форк репозиторію** та створіть нову гілку.
3.  **Внесіть зміни** у каталозі `packages/`.
4.  **Переконайтеся, що всі перевірки пройдено**, запустивши
    `npm run preflight`.
5.  **Відкрийте pull request** зі своїми змінами.

### Огляд коду (Code reviews)

Усі пропозиції, включаючи пропозиції від членів проекту, потребують перевірки.
Для цього ми використовуємо
[GitHub pull requests](https://docs.github.com/articles/about-pull-requests).

Якщо ваш pull request стосується змін у `packages/cli` (фронтенд), ми
рекомендуємо запустити наш інструмент автоматичного огляду фронтенду.
**Примітка: цей інструмент наразі є експериментальним.** Він допомагає виявити
типові помилки в React, проблеми з тестуванням та інші специфічні для фронтенду
моменти.

Щоб запустити інструмент огляду, введіть таку команду всередині Gemini CLI:

```text
/review-frontend <НОМЕР_PR>
```

Замініть `<НОМЕР_PR>` номером вашого pull request. Авторам рекомендується
запускати це для самоперевірки, а рецензенти можуть використовувати це як
додаток до ручного огляду.

### Призначення завдань собі

Щоб призначити issue собі, просто додайте коментар із текстом `/assign`. Команда
повинна містити лише цей текст. Ця команда закріпить завдання за вами, якщо воно
ще ніким не зайняте.

Зверніть увагу, що одночасно за вами може бути закріплено не більше 3 завдань.

### Правила оформлення Pull Request

Щоб ми могли швидше переглянути та прийняти ваші PR, дотримуйтесь цих правил.
PR, які їм не відповідають, можуть бути закриті.

#### 1. Посилання на існуючу проблему

Усі PR повинні бути пов'язані з існуючою проблемою (issue) у нашому трекері. Це
гарантує, що кожна зміна була обговорена та відповідає цілям проекту.

- **Для виправлення помилок:** PR має бути пов'язаний зі звітом про помилку (bug
  report).
- **Для нових функцій:** PR має бути пов'язаний із запитом на функцію (feature
  request), схваленим розробниками проекту.

Якщо проблеми для вашої зміни ще немає, будь ласка, **спочатку створіть її** та
дочекайтеся відгуку.

#### 2. Маленькі та фокусовані зміни

Ми віддаємо перевагу невеликим, атомарним PR, які вирішують одну проблему або
додають одну завершену функцію.

- **Так:** Створити PR, який виправляє одну конкретну помилку.
- **Ні:** Змішувати кілька непов'язаних змін (наприклад, виправлення помилки,
  нова функція та рефакторинг) в одному PR.

Великі зміни слід розбивати на серію менших логічних PR, які можна перевіряти
незалежно.

#### 3. Використовуйте Draft PR для незавершеної роботи

Якщо ви хочете отримати ранній відгук, використовуйте функцію GitHub **Draft
Pull Request**. Це сигнал для нас, що PR ще не готовий до фінального огляду, але
відкритий для обговорення.

#### 4. Проходження перевірок

Перед подачею PR переконайтеся, що всі автоматичні перевірки пройдено,
запустивши `npm run preflight`. Ця команда запускає тести, лінтер та перевірку
стилю.

#### 5. Оновлення документації

Якщо ваш PR вносить зміни, які бачить користувач (нова команда, змінений
прапорець або зміна поведінки), ви також повинні оновити документацію в каталозі
`/docs`.

Докладніше:
[Процес внесення змін у документацію](#процес-внесення-змін-у-документацію).

#### 6. Чіткі назви комітів та опис PR

Ваш PR повинен мати зрозумілу назву та детальний опис змін. Дотримуйтесь
стандарту [Conventional Commits](https://www.conventionalcommits.org/).

- **Гарна назва:** `feat(cli): Add --json flag to 'config get' command`
- **Погана назва:** `Made some changes`

В описі поясніть "чому" ви зробили ці зміни та додайте посилання на issue
(наприклад, `Fixes #123`).

### Форкінг (Forking)

Якщо ви зробили форк репозиторію, ви зможете запускати збірку та тести. Однак
для роботи інтеграційних тестів вам знадобиться додати
[GitHub Repository Secret](https://docs.github.com/en/actions/security-for-github-actions/security-guides/using-secrets-in-github-actions#creating-secrets-for-a-repository)
з назвою `GEMINI_API_KEY` та вашим ключем API. Ваші секрети залишаються
приватними.

Також вам потрібно буде увімкнути GitHub Actions у вкладці `Actions` вашого
форку.

## Налаштування середовища розробки

Цей розділ допоможе вам розібратися зі структурою проекту та його збіркою.

### Підготовка середовища

**Вимоги:**

1.  **Node.js**:
    - **Для розробки:** Використовуйте Node.js `~20.19.0`. Ця версія необхідна
      через специфічні залежності. Рекомендуємо
      [nvm](https://github.com/nvm-sh/nvm) для керування версіями.
    - **Для використання:** Будь-яка версія Node.js `>=20`.
2.  **Git**

### Процес збірки

Клонуйте репозиторій:

```bash
git clone https://github.com/santoni-star/gemini-cli-uk.git
cd gemini-cli-uk
```

Встановіть залежності:

```bash
npm install
```

Зберіть проект:

```bash
npm run build
```

Ця команда компілює TypeScript, збирає ресурси та готує пакунки до роботи.

### Увімкнення пісочниці (Sandboxing)

Ми наполегливо рекомендуємо використовувати [пісочницю](#пісочниця). Для цього
встановіть `GEMINI_SANDBOX=true` у вашому `~/.env` та переконайтеся, що у вас
встановлено `docker`, `podman` або використовується `macOS Seatbelt`.

Щоб зібрати CLI разом із контейнером пісочниці, запустіть:

```bash
npm run build:all
```

### Запуск CLI

Щоб запустити Gemini CLI з вихідного коду:

```bash
npm start
```

### Запуск тестів

#### Юніт-тести

```bash
npm run test
```

#### Інтеграційні тести

```bash
npm run test:e2e
```

Більше інформації у
[документації до інтеграційних тестів](/docs/integration-tests.md).

### Перевірки перед комітом (Preflight)

```bash
npm run preflight
```

Ця команда запустить лінтер, форматування та всі тести.

## Стандарти кодування

- Дотримуйтесь існуючого стилю та шаблонів коду.
- Дивіться
  [GEMINI.md](https://github.com/google-gemini/gemini-cli/blob/main/GEMINI.md)
  для інструкцій щодо розробки з використанням AI, включаючи правила для React
  та Git.
- **Імпорти:** Звертайте увагу на шляхи імпорту. Проект обмежує використання
  відносних імпортів між пакетами через ESLint.

## Структура проекту

- `packages/`: Містить підпроекти.
  - `a2a-server`: Реалізація сервера A2A (експериментально).
  - `cli/`: Командний інтерфейс.
  - `core/`: Основна логіка бекенду.
  - `test-utils`: Утиліти для тестування.
  - `vscode-ide-companion`: Розширення для VS Code.
- `docs/`: Документація проекту.
- `scripts/`: Сценарії для збірки та розробки.

## Процес внесення змін у документацію

Документація має бути чіткою, лаконічною та актуальною.

### Як почати

Процес схожий на роботу з кодом:

1. Зробіть форк та створіть гілку.
2. Внесіть зміни у каталозі `/docs`.
3. Перевірте вигляд Markdown локально.
4. Запустіть `npm run preflight`.
5. Відкрийте pull request.

---

<p align="center">
  Дякуємо за ваш внесок у розвиток Gemini CLI! ❤️
</p>
